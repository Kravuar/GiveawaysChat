import {Stack, Typography} from "@mui/material";
import GiveawaysContainer from "../components/parts/GiveawaysContainer";

export default function Home() {

    return (
        <Stack>
            <Typography>HOME</Typography>
            <GiveawaysContainer/>
        </Stack>
    );
}